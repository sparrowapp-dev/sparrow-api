{{!< layoutName}} <!DOCTYPE html>
  <html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sparrow Reset Password</title>


    <style>
      body {
        font-family: "Inter", sans-serif;
        box-sizing: border-box;
        padding: 0;
        margin: 0;
        transition: background-color 0.3s, color 0.3s;
      }

      .container {
        display: flex;
        justify-content: center;
        width: 100%;
        height: 100%;

      }

      .container-data {
        margin: auto;
        max-width: 570px;
        background-color: var(--background-color, white);
        padding: 20px;
        transition: background-color 0.3s, color 0.3s;
      }

      .header-container {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
      }

      .sparrow-heading-text {
        font-size: 40px;
        font-weight: 800;
        color: var(--header-text-color, #3670F7);
      }



      .email-text-style {
        color: var(--email-text-color, #333);
        font-family: "Inter", sans-serif;
        font-size: 14px;
        font-style: normal;
        font-weight: 400;
        line-height: 21px;
      }

      .text-bold-dynamic {
        color: var(--text-bold-color, #333333);
        font-weight: 700;
        line-height: 21px;
      }

      .icon {
        transition: filter 0.3s;
      }

      .dark-mode .icon {
        filter: invert(100%);
      }
    </style>
  </head>

  <body>

    <div class="container">
      <section style="width: 100%;">
        <div class="container-data">

          {{> header}}

          <section>

            <h1>Dear {{name}} </h1>
            <p>We have Recieved Your Request to reset your Password.Please use the verification code below to complete
              the rest </p>
            <p>The verification code is valid for 1 min</p>
            <p style="color: blue;       /* Set the text color to blue */
    font-size: 20px; ">{{verificationCode}}</p>
            <p>The verification code is valid for 1 min</p>
            <p>Please do not share verification code</p>
            <p>If you need additional assistance or you did not make this change, please contact us at <a
                href="mailto:{{sparrowEmail}}" style="text-decoration-line: underline;">{{sparrowEmail}}</a></p>

            <hr>

          </section>


          {{> footer}}


        </div>
      </section>
    </div>
  </body>

  </html>